
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      // Allow anyone to read, update, or delete a specific game document.
      allow get, update, delete: if true;
    }
    match /games/{gameId} {
        // Allow anyone to create a new game document.
        allow create: if true;
    }
  }
}
