rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read, create, update, and delete game documents.
    // This is suitable for a public, anonymous game.
    match /games/{gameId} {
      allow read, write: if true;
    }
  }
}
