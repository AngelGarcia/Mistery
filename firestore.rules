rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      // Allow anyone to read a game document.
      // This is necessary for the onSnapshot listener to work.
      allow get: if true;
    }
  }
}
