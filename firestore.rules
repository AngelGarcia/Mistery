rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow clients to create new game documents
    match /games/{gameId} {
      allow create, read, update, delete: if true;
    }
  }
}
