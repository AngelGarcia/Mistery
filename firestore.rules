rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      // Allow anyone to read or write to the games collection.
      // This is necessary for creating, joining, and playing the game
      // without requiring user authentication.
      allow read, write: if true;
    }
  }
}
